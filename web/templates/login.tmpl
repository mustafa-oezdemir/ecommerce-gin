{{define "login.tmpl"}}<!doctype html><html><body><h1>Giriş Yap</h1>{{if .error}}<p role="alert">{{.error}}</p>{{end}}<form method="post" action="/login">{{.CSRFField}}<label>Email <input type="email" name="email" required autocomplete="email"></label><label>Şifre <input type="password" name="password" required autocomplete="current-password"></label><button type="submit">Giriş</button></form></body></html>{{end}}
