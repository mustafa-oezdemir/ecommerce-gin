{{define "partials/empty-state"}}<div class="empty-state"><h2>{{.Title}}</h2>{{if .Description}}<p>{{.Description}}</p>{{end}}{{if .ActionURL}}<a class="btn btn-primary" href="{{.ActionURL}}">{{.ActionLabel}}</a>{{end}}</div>{{end}}
