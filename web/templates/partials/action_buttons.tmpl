{{define "partials/action-buttons"}}<div class="action-buttons">{{if .ViewURL}}<a class="btn btn-sm btn-outline-secondary" href="{{.ViewURL}}">View</a>{{end}}{{if .EditURL}}<a class="btn btn-sm btn-outline-primary" href="{{.EditURL}}">Edit</a>{{end}}{{if .DeleteDialog}}<button class="btn btn-sm btn-outline-danger" type="button" data-dialog-open="{{.DeleteDialog}}">Delete</button>{{end}}</div>{{end}}
