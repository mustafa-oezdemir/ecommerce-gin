{{define "partials/page-header"}}<header class="page-header"><div><h1>{{.Title}}</h1>{{if .Description}}<p>{{.Description}}</p>{{end}}</div>{{if .ActionURL}}<a class="btn btn-primary" href="{{.ActionURL}}">{{.ActionLabel}}</a>{{end}}</header>{{end}}
