global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: ecommerce-app
    static_configs:
      - targets: ["app:9091"]

  - job_name: shipping-service
    static_configs:
      - targets: ["shipping-app:9092"]
